<h1>Stages</h1>

<div class="stages-content">
  <% @stages.each do |stage| %>
    <div class="stage-card">
      <div class="stage-img"><%= image_tag stage.image_url %></div>
      <h2><%= link_to stage.name, stage_path(stage.id) %></h2>
    </div>
  <% end %>
</div>
